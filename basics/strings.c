#include <string.h>
#include <stdlib.h>
#include <stdio.h>
#include <linux/audit.h>

int main(int argc, char** argv){
  // argc denotes the size of total memory denoting the command line arguments
  // argv is a pointer to start of characters that represent the command line arguments
  puts("Hello World"); // pushing char array to standard out
}
